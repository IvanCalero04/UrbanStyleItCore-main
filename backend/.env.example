PORT=3000
DATABASE_URL=postgres://urbanstyle:urbanstyle_password@db:5432/urbanstyle
JWT_SECRET=change_this_to_a_long_random_secret

# Set to "true" if your Postgres requires SSL (e.g., some managed services)
PGSSL=false
